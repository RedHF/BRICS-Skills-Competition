# v10 验证结果

- `go-test.log`：Go 全套测试通过，包含首领血量、攻击节奏、第三技能触发顺序与服务端判定。
- `journey.log`：实际 GUI 操作完整通过五章十任务；主键盘 1–5、数字小键盘、未习得技艺、重复键、冷却、首领切换和结算通过。较慢操作的首领战耗时 41.25 秒。
- `audio.log`：184 段资源有效；实际完成信号驱动语音队列、配乐压低与恢复、回顾剧情时的独立音频、静默句、心舍新回声和暂停通过。
- `narrative.log`：对话推进、上一句、跳过、返回、四类失败首句、九句门槛、续读、真实免费回溯通过。
- `release-verification.json`：最终 ZIP 校验值；在独立临时目录解压，使用独立 APPDATA，运行实际发行 EXE；本地服务自动启动、v10 内容、184 段配音、六项采用图像、排除假透明人物、语音与动画、退出清理均通过。

发行版检查使用显式失败退出，避免 release 构建忽略 GDScript 的 assert。首次检查发现循环 WAV 播放器在退出时可能残留，已通过先解除暂停和循环、再停止并释放音轨修复。最终解压运行无脚本错误、无资源泄漏告警，配套服务随客户端正常退出。

截图包括四个新调查场景、首领出场、战斗按钮、剧情和失败界面。本轮测试使用独立存档，没有重置用户实际游戏进度。

最终发行包：`dist/檐下千秋-v10-Windows-x64-20260914.zip`，90,172,656 字节（约 86 MiB）。

SHA256：`b69bc7e367947c7cac0ebf8a7ba94fc5a1d714bee7dfc453d79532b066c92f94`。
