# 《檐下千秋》服务端机制逆推报告

**证据基线**：`LLM-tmp/服务端/` 全部 Go 源码（`internal/rules`、`internal/httpapi`、`internal/model`、`internal/store`、`internal/content`、`cmd/server`）+ `content/chapters.json`（内容协议 v10）+ `content/README.md` + 各 `_test.go`。
行号均为该次读取时的实际行号；未在代码中出现的行为一律不写入正文，仅在第 11 节登记为落差。

---

## 0. 事实基线

| 项 | 值 | 依据 |
|---|---|---|
| 游戏 ID | `yanxia-qianqiu` | `chapters.json` 根 `game_id`；校验 `content.go:211-213` |
| 内容协议版本 | **10** | `content.go:14 CurrentVersion = 10`；严格相等校验 `content.go:208-210` |
| 构建标识 | `assets-boss-v10` | `server.go:54` |
| 章节 / 事件 / 墨灵 / 战斗 | 5 / 10 / 10 / 8 | `chapters.json`；`content_test.go:49` |
| 玩家模型 | 单存档单玩家（服务端启动即绑定 local player） | `server.go:50-54`，`store.go:379-405` |
| 认证 | 无。所有路由不校验 token；非本机 player_id 一律 403 | 路由表 `server.go:59-88`；`server.go:218-221`、`274-277`、`420-423` |
| 官方客户端地址 | `127.0.0.1:8090` | `main.go:26` |

---

## 1. 核心循环与状态机

### 1.1 循环骨架

`POST /api/v1/players` → `POST /api/v1/sessions` → `POST /sessions/{id}/puzzle` ×N → `POST /sessions/{id}/choice` → [`POST /sessions/{id}/battle`，仅配置了 `battle` 的事件] → `POST /sessions/{id}/finish` → 若 `failed` 则 `POST /sessions/{id}/rewind` 回起点。

路由与别名（`server.go:59-88`、`441-474`）：

| 规范路由 | 别名（同一 handler） |
|---|---|
| `POST /api/v1/sessions` | `/api/v1/runs` |
| `/sessions/{id}/puzzle` | `/actions` |
| `/sessions/{id}/choice` | `/memory` |
| `/sessions/{id}/finish` | `/submit`、`/settle` |
| `POST /api/v1/sessions` | `POST /api/v1/events/{chapter}/{event}/start`（`server.go:973-990`） |

### 1.2 会话状态字段

`EventSession.Status` 注释声明四态：`active, awaiting_choice, completed, failed`（`model.go:76`）。

| 状态 | 由谁写入 | 依据 |
|---|---|---|
| `active` | 创建会话 / 回溯重置 | `server.go:395`、`rewind.go:44` |
| `completed` | 结算事务 | `server.go:897`、`913` |
| `failed` | 谜题尝试耗尽 / 侵蚀达上限 / 战斗未胜 | `rules.go:165`、`server.go:558`、`665`、`670` |
| `awaiting_choice` | **从未写入**（仅 `store.Snapshot` 统计时被识别） | `store.go:285`（详见第 11 节） |

失败原因 `FailureReason` 取值：`puzzle_attempt_limit`（`rules.go:166`）、`erosion_limit`（`server.go:559`、`671`）、`battle_requirements_not_met`（`server.go:666` ← `rules.go:268`）。

### 1.3 步骤类型

谜题判定实际分支（`rules.go:102-137`）：

| `kind` | 判定逻辑 | 依据 |
|---|---|---|
| `trace` | 原始指针轨迹逐笔比对，无字符串答案 | `rules.go:112-120` + `trace.go:14-26` |
| `skill` | ①`attempt.Target` 必须等于 `step.Target`；②`attempt.Answer` 必须等于 `step.Answer`（技能名） | `rules.go:121-123` + `135-137` |
| `narrative` | `attempt.Answer` 必须命中 `step.Options` 之一；写入 `session.NarrativeChoice` | `rules.go:124-134` |
| 其它（`join`、`choice`、以及任意字符串） | 走通用分支：`answer` 字符串比较（大小写不敏感 + trim） | `rules.go:135-137`、`291-293` |

`chapters.json` 实际使用的 `kind`：`trace`/`skill`/`narrative`/`join`/`choice`（5 种）；`puzzle.type` 字段（`trace`/`join`/`purify_trace`/`pattern`/`combo`/`choice`）**不参与任何服务端逻辑**，只随公开目录下发（`server.go:160`）。

### 1.4 状态流转（含守卫）

| 步骤 | 前置守卫 | 失败响应 |
|---|---|---|
| 开始事件 | 事件存在、非 `draft`、章节已解锁、**同章内 `order` 更小的所有事件都已 `completed`** | 403 `chapter_locked` / 409 `draft_event` / 409 `event_locked`（`server.go:367-385`） |
| 开始事件（重复） | `Store.StartSession` 会返回该玩家最近一个非 `completed` 会话 | 不再新建（`store.go:163-166`；测试 `server_test.go:107-111`） |
| 谜题 | 会话非 `failed`；步骤按 `AcceptedSteps` 长度索引，只能交下一步 | 409 `session_failed`（`server.go:529-535`）；乱序走 `rules.go:109-111` |
| 抉择 | 谜题全部完成 | 409 `puzzle_incomplete`（`server.go:725-728`） |
| 战斗 | 会话非 `failed`、谜题全部完成、**抉择已完成** | 409 `puzzle_incomplete` / 409 `choice_incomplete`（`server.go:621-633`） |
| 结算 | 会话非 `failed`、谜题全部完成、`battle` 存在时必须 `BattleChecked && BattleWon`、抉择已完成 | 409 `battle_incomplete` / `choice_incomplete`（`server.go:866-881`） |
| 回溯 | 会话必须处于 `failed` 且 `retries` 匹配 | 409 `invalid_retry`（`rewind.go:30-32`） |

**顺序是强制的**：抉择必须在战斗之前（`server.go:630-633`），结算必须在抉择之后（`server.go:878-881`）。

### 1.5 幂等分支（同一请求重放不会二次记账）

| 场景 | 行为 | 依据 |
|---|---|---|
| 已 `completed` 会话再交谜题 | `accepted=true, complete=true, reason=already_completed` | `rules.go:62-70` |
| 重放已接受的 `step_id` | `reason=step_already_accepted`，不重复加分 | `rules.go:89-100` |
| 谜题已全部接受 | `reason=puzzle_complete` | `rules.go:77-85` |
| 战斗重复提交 | 返回既有结果，`reason=already_checked`，事务内提前 `return nil` | `server.go:615-619`、`653-656` |
| 抉择重复提交 | `reason=already_checked`，事务内提前 `return nil` | `server.go:717-720`、`735-737` |
| 结算重复提交 | `reason=already_settled`，返回同一 `EventResult`（`sequence` 不变） | `server.go:860-864`、`885-888`；测试 `server_test.go:68-76` |
| 已通关事件再次结算 | 直接返回存档中的旧结果，**不加墨痕、不重复写入账册** | `server.go:893-899` |
| 回溯重复提交（相同 `retries`） | `current.Retries == retries+1` → 直接 `return nil`，不重复扣费 | `rewind.go:27-29`；测试 `rewind_test.go:52-60` |
| `failed` 会话不可再操作 | 409 `session_failed`（puzzle/battle） / 409 `session_failed`（finish） / 409 `session_failed`（choice） | `server.go:533`、`607`、`867`、`722` |

事件**没有墙钟超时**：`ExpiresAt` 从不在服务端赋值（`server.go:392-398` 未设置），过期时间下发恒为零值；`server_test.go:125-160` 明确断言「把 `ExpiresAt` 设为过去一分钟仍可正常通关」。

---

## 2. 经济系统（墨痕 InkMarks）

### 2.1 获取

| 来源 | 数量 | 依据 |
|---|---|---|
| 事件结算 | **+星数**（1 星 = 1 墨痕，2 星 = 2，3 星 = 3） | `server.go:970 InkMarksEarned: stars`；`server.go:905 player.InkMarks += result.InkMarksEarned` |
| 重玩已完成事件 | **0**（返回旧结果，不进结算分支） | `server.go:893-899` |

星数由综合分决定（见第 8 节），因此单事件墨痕 ∈ {1,2,3}。
测试基线：2 章 4 个事件全 3 星 → 12 墨痕，且不因章节解锁被扣（`server_test.go:359-361`）。

### 2.2 消耗

| 去向 | 数量 | 依据 |
|---|---|---|
| 回溯（rewind） | **1 点**，仅当 `InkMarks > 0` 时扣除；为 0 时免费 | `rewind.go:33-36` |
| 章节解锁 | 读 `next.UnlockCost`，从 `InkMarks` 扣减 | `server.go:1071-1077` |

`InkMarks` 被归一化下限为 0（`store.go:304-306`），不会出现负数。

### 2.3 章节解锁费用

解锁是**自动**的，没有单独的购买接口：结算时 `unlockNextChapter` 检查「本章所有事件已完成」+「`InkMarks >= next.UnlockCost`」+「`next.Events[0]` 非 draft」，满足则扣费并追加到 `UnlockedChapters`（`server.go:1060-1079`）。

| 章节 | `order` | `unlock_cost` | `next_chapter` |
|---|---|---|---|
| `prologue` 序章·风雨廊桥 | 0 | 0 | `temple` |
| `temple` 第一章·社庙 | 1 | 0 | `opera` |
| `opera` 第二章·戏楼 | 2 | 0 | `archway` |
| `archway` 第三章·牌坊 | 3 | 0 | `tower` |
| `tower` 第四章·通天塔 | 4 | 0 | （无） |

**当前版本全部为 0**，即扣费路径存在但实际不产生任何消耗；`UnlockCost < 0` 在加载期被拒绝（`content.go:273-275`）。
另：`RewardSpec.UnlockChapter`（`content.go:154`，JSON 键 `reward.unlock_chapter`）在全部代码中**无任何引用**——仅字段（第 11 节）。

结论：**v10 的墨痕是净收入资源，唯一消耗口是回溯 1 点**，全流程最坏消耗 = 回溯次数。

---

## 3. 容量系统（心舍容量）

| 项 | 值 | 依据 |
|---|---|---|
| 初始容量 | **5** | `store.go:369`（`newPlayer`）；兜底 `Capacity <= 0 → 5`（`store.go:301-303`） |
| 容量上限 | **7**（常量 `maxCapacity`） | `server.go:26`；扩容时 `clamp(..., 1, maxCapacity)`（`server.go:907`） |
| 单位 | 墨灵 `capacity` 格，当前 10 个墨灵**全部为 1 格** | `chapters.json memories[].capacity` |
| 占用计算 | `usedCapacity = Σ memory.Capacity` | `server.go:1006-1012` |
| 扩容时机 | **结算事务内**，`Reward.CapacityIncrease > 0` 时叠加 | `server.go:906-908` |
| 扩容配置 | `temple/temple_drum` +1（→6）、`archway/archway_craftsman` +1（→7） | `chapters.json reward.capacity_increase` |

扩容发生在**该事件结算之后**（`handleFinish` 内），因此第 4 个事件（社鼓声）的抉择仍按 5 格判定。

### 满容量抉择流程（`validateChoice`，`server.go:780-833`）

1. 必须有待收墨灵 `PendingMemory`（会话创建时由事件奖励生成，`server.go:397`），否则 400 `invalid_choice`（`780-783`）。
2. `action` 允许三种写法：字面量 `keep`、字面量 `forget`、或**事件 `reward.choices` 中出现的任意选项**（`784-790`）。
3. 分支：
   - `forget`：必须提供 `forget_memory_id`，且该 ID 必须已存在于 `player.Memories`（`791-805`）。
   - 其它非 `keep` 的选项：内容约定用「已持有的墨灵 ID」作为紧凑的忘却选项，命中则等价于忘掉它（`806-816`，注释明确举例 `temple_drum`）。
4. 禁止「忘掉正要获得的那一个」（`818-820`）。
5. 可用容量 = `player.Capacity - usedCapacity(player)`，若本次要忘掉某个墨灵则先 `+= 该墨灵 capacity`（`817-828`）。
6. 若待收墨灵尚未持有且 `PendingMemory.Capacity > 可用容量` → 400 `invalid_choice`，文案「先遗忘一个已有墨灵」（`829-831`）。

**结果落库**（`server.go:742-761`）：置 `ChoiceDone/ChoiceAction/ForgottenMemory`；若该事件此前**未通关**，则原子地执行「移除被忘墨灵 → 写 `forgotten` 账册 → 追加 `PendingMemory` 到 `player.Memories` → 写 `kept` 账册」。若该事件已通关，则不再改动记忆集合（防止重玩刷墨灵）。

**事务性保证**：抉择、扣容量、账册、记忆集合全部在**一次 `UpdateSessionAndPlayer`** 内完成——单次 `persistLocked()`（临时文件写入 + `os.Rename` 原子替换），任何一步返回 error 都整体回滚内存态（`server.go:734-763` → `store.go:252-279`、`72-88`）。

---

## 4. 侵蚀度系统（Erosion）

### 4.1 全部增减数值

| 事件 | 增减 | 依据 |
|---|---|---|
| 非描摹谜题失败（乱序 / 错答案 / 错目标 / 非法叙事选项） | **+10** | `rules.go:19 PuzzleFailureErosion = 10`；`rules.go:161` |
| 描摹失败（含缺笔、顺序错、超容差） | **0**（不加侵蚀、不计入 `InvalidAttempts`、不计入 `MaxAttempts`） | `rules.go:115-120` 直接 return，未调 `rejectPuzzle`；测试 `server_test.go:60-63`、`rules_test.go:17` |
| 战斗中每被击中一次 | **+5** | `rules.go:21 HitErosion = 5`；`rules.go:265 ErosionDelta = hits * HitErosion` |
| 战斗未胜（未清完波次 / 缺必需技能 / 受击超限 / 战斗中触顶） | **额外 +15** | `rules.go:20 BattleFailureErosion = 15`；`rules.go:266-269` |
| 结算时 `player.Erosion >= 70` | 不改变侵蚀，但**综合分 −15** | `server.go:956-958` |
| 会话创建 | 快照 `StartErosion = 当前 Erosion` | `store.go:167` |
| 回溯 | `Erosion = StartErosion`（唯一降低途径） | `rewind.go:37` |

所有侵蚀增量经 `clamp(..., 0, maxErosion)`：上限 **100**（`server.go:25`、`555`、`668`），存档归一化同样钳制 `0..100`（`store.go:307-312`）。
被判为「非法战斗提交」的请求（时间戳越界、冷却中使用、未持有技能）**不产生任何侵蚀**且不写库（`rules.go:205-225` 直接返回 error → `server.go:649-652` 返回 400 `invalid_battle`）。

### 4.2 阈值

服务端**只有两个阈值**：

| 阈值 | 效果 | 依据 |
|---|---|---|
| `>= 70` | 结算综合分 −15（最多扣一次） | `server.go:956-958` |
| `>= 100` | 该事件立即判负 `erosion_limit`；钳制到 100 | `server.go:557-560`（谜题路径）、`669-672`（战斗路径）、`store.go:310-312` |

战斗内部另有**硬停止**判定：`StartErosion + hits*5 >= 100` 时停止推进攻击、禁止后续技能，并直接判负（`rules.go:220`、`226`、`234`、`253`、`261`）。

> 「三阶段阈值」**在服务端不存在**。三档只在 Godot 客户端做展示切分：`<40` 显示「浸」/绿、`40–69` 显示「蚀」/黄、`>=70` 显示「竭」/红（`客户端/scripts/main.gd:301-309`，`building_scene.gd:101-108`，测试 `客户端/tests/integration.gd:65-70`）。服务端对 40 这个分界没有任何逻辑。

### 4.3 达 100 的后果

1. 玩家级 `Erosion = 100`（钳制）。
2. 当前会话置 `failed`、`FailureReason = "erosion_limit"`（`server.go:557-560`、`669-672`）。
3. `failed` 会话被**冻结**：puzzle / battle / choice / finish / 再开新会话全部被拒（`server.go:529-535`、`606-609`、`721-724`、`866-869`；`store.go:163-166` 使 `POST /sessions` 返回同一失败会话）。
4. 唯一出路是 `POST /sessions/{id}/rewind`；回溯后 `Erosion` 回落到本次事件开始时的 `StartErosion`（`rewind.go:37`）。

注意：`erosion_limit` 的失败演出文案由内容提供（`chapters.json failure_scene.first_lines.erosion_limit`，校验见 `content.go:202-207`）。

---

## 5. 战斗系统

### 5.1 技能表（`chapters.json skills`，结构见 `content.go:17-24`；校验 `content.go:220-224`）

| 技能 | 伤害 | 护盾 | 治疗 | 冷却 (ms) | 颜色 | 备注 |
|---|---|---|---|---|---|---|
| 挥墨 | 10 | 0 | 0 | 500 | `#e4c98a` | 必备基础攻击，加载期强制存在（`content.go:217-219`） |
| 闪身 | 0 | 1 | 0 | 2500 | `#b7e7df` | 无任何墨灵授予，但被硬编码为始终可用（`server.go:636`）；没有任何 `required_skills` 使用它 |
| 斗拱 | 0 | 2 | 0 | 6000 | `#9ad5ad` | 由 檐·平安 / 社·承香 / 坊·匠魂 授予 |
| 飞檐 | 25 | 0 | 0 | 4000 | `#8dcfe3` | 由 戏·行腔 / 戏·绝唱 / 坊·无名 授予 |
| 藻井 | 18 | 0 | 1 | 6000 | `#d7b1eb` | 由 社·宁安 / 社·回春 / 塔·初代之忆 授予 |

技能生效语义（`rules.go:237-244`）：

- `hp -= skill.Damage`
- 护盾：`shield = max(shield, skill.Shield)` —— **取最大值而非叠加**，且护盾只在每次敌人攻击时消耗 1 点（`rules.go:226-233`）。
- 治疗：`hits = max(0, hits - skill.Heal)` —— 效果是**冲抵已受击次数**（等价于回血并返还侵蚀额度的扣减额度），不是加血上限。
- 冷却：`ready[skill] = at_ms + CooldownMS`，秒表走的是客户端提交的 `at_ms` 时间轴。

技能可用性（服务端裁剪，`server.go:634-642`）：`playerSkills(player)` = 账册中 `action == "kept"` 的墨灵技能 ∪ 当前持有墨灵的技能（`server.go:1014-1030`），再**无条件加入 `挥墨` 与 `闪身`**。请求中出现未持有技能 → 400 `skill_unavailable`。

### 5.2 敌人配置（逐事件）

普通波攻击间隔**硬编码 3000 ms**（`content.go:143-148`）；`max_hits_taken` 全部为 12；`duration_sec` 全部为 60。

| 事件 | waves | enemy_hp | 首领 | 必需技能 |
|---|---|---|---|---|
| prologue/prologue_bridge | 1 | 640 | — | 斗拱 |
| temple/temple_incense | 1 | 680 | — | 斗拱 |
| temple/temple_drum | 1 | 740 | — | 藻井 |
| opera/opera_opening | 2 | 300 | **白蚀·噤声客** HP 620，攻击间隔 2400 ms | 斗拱, 藻井, 飞檐 |
| opera/opera_master | 1 | 1000 | — | 飞檐 |
| archway/archway_form | 2 | 520 | — | 飞檐 |
| archway/archway_craftsman | 2 | 540 | — | 斗拱, 飞檐 |
| tower/tower_ascent | 3 | 360 | — | 斗拱, 飞檐, 藻井 |
| temple/temple_guest、tower/tower_ledger | 无 `battle` 字段 | | | |

### 5.3 首领战

- 首领 = 最后一波，且必须配置 `boss_name`：`IsBoss(wave) = BossName != "" && wave == Waves-1`（`content.go:132-134`）。
- 血量：`WaveHP(wave)` → 首领波用 `boss_hp`，普通波用 `enemy_hp`（`content.go:136-141`）。
- 只有 `opera/opera_opening` 是首领战（全目录 1 场，`boss_test.go:35-37` 强制断言）。
- **首领出场重置预警**：进入首领波的瞬间 `nextAttack = action.AtMS + AttackInterval(waves)`（`rules.go:248-250`），即首领的第一击从该波开始时刻起算 2400 ms。
- 加载校验：`boss_name` 非空时要求 `boss_hp > 0` 且 `boss_attack_interval_ms >= 1000`（`content.go:345-347`）。

### 5.4 提交与校验（服务端全量重放）

请求：`{player_id?, actions:[{skill, at_ms}], duration_ms, waves_cleared?, hits_taken?}`（`server.go:579-590`）。
服务端**忽略** `waves_cleared` 与 `hits_taken`（写入 `BattleInput` 后 `EvaluateBattle` 从不读取，见 `rules.go:179-190`）。

重放算法（`rules.go:200-271`）：

1. 无 `battle` 配置 → 直接 `Won=true, reason=no_battle`（`201-203`）。
2. `duration_ms` 必须满足 `0 < d <= duration_sec*1000`（=60000），否则 400（`205-207`）。
3. 初始 `hp=WaveHP(0)`、`waves=0`、`hits=0`、`shield=0`、`nextAttack=AttackInterval(0)`（=3000 ms）（`208`）。
4. 逐动作：技能必须在目录且已持有（`213-216`）；`at_ms` 必须单调不减且落在 `[0, duration_ms]`（`217-219`）；战斗已结束则报错（`220-222`）；冷却检查（`223-225`）；先把敌人攻击推进到 `at_ms`（护盾优先吸收，`226-233`）；再次检查战败（`234-236`）；然后是技能效果与波次推进（`237-251`）。
5. 剩余时间继续推进敌人攻击，直到 `duration_ms` 或战败（`253-260`）。

### 5.5 胜负判定

```
won = (waves == battle.Waves)
   && (hits <= battle.MaxHitsTaken)
   && (StartErosion + hits*5 < 100)
   && 每个 required_skills 都至少施展过一次
```
依据 `rules.go:261-264`（`seen` 集合在 `239` 行记录）。

- 胜：`reason = "battle_cleared"`（`265`）。
- 负：`reason = "battle_requirements_not_met"`，`ErosionDelta = hits*5 + 15`，会话置 `failed`（`266-269`、`server.go:664-667`）。
- 结果落库字段：`BattleChecked/BattleWon/BattleWaves/BattleHits`（`server.go:657-660`）。
- 时限：`duration_sec = 60` 秒为硬上限；客户端与测试目标是 30–60 秒内可打完（`rules/review_test.go:42-73` 对全部 8 场战斗用 500 ms / 750 ms 两种出手间隔断言 30 秒 ≤ 用时 < 60 秒）。

---

## 6. 回溯系统（rewind / checkpoint）

端点：`POST /api/v1/sessions/{id}/rewind`，body `{retries: <整数>}`（`server.go:448-453`，`rewind.go:13-23`）。`retries` 是**客户端持有的乐观版本号**，必填且 `>= 0`。

### 6.1 费用

| 情形 | 费用 | 依据 |
|---|---|---|
| `player.InkMarks > 0` | **1 墨痕** | `rewind.go:34-36` |
| `player.InkMarks == 0` | **免费**（注释原文：失败会话会阻塞推进，因此每个章节都有零墨痕兜底） | `rewind.go:33-36`；测试 `rewind_test.go:73-91` |

代码层面**没有「哪些章节免费」的白名单**：免费与否只看钱包是否为 0。当前所有章节解锁费为 0（第 2.3 节），因此实际表现是「有墨痕就扣 1，没有就不扣」。

### 6.2 恢复的状态（`rewind.go:41-47`）

- `player.Erosion = session.StartErosion`（回到本事件开始时的侵蚀）
- `PuzzleTotal` 依内容重算
- `AcceptedSteps = []`（谜题进度清空）
- `Actions = []`（操作记录清空）
- `PendingMemory = memoryFromEvent(event)`（重新挂上待收墨灵）
- `Status = "active"`
- `Retries = 旧值 + 1`
- 隐式归零：`PuzzleScore`、`AttemptCount`、`InvalidAttempts`、`BattleWon/BattleChecked/BattleWaves/BattleHits`、`RepairPercent`、`FailureReason`、`RewardApplied/PendingResult`（结构体字面量未赋值）

保留：`ID/PlayerID/ChapterID/EventID/StartedAt/StartErosion/LastSeenAt`。

### 6.3 不恢复的状态

- **已获得的墨灵**（`player.Memories`）与**记忆账册**（`player.MemoryLedger`）——注释与测试明确「失败事件恢复起点侵蚀与谜题并保留已获得墨灵」（`rewind.go:33-34`、`rewind_test.go:60`）。
- **已扣的墨痕**（只扣不还）。
- **已完成的抉择**：`ChoiceDone / ChoiceAction / ForgottenMemory` 被显式复制到新会话（`rewind.go:39-40`、`46`）。理由见注释：保留已提交的抉择，避免已记账的奖励在重试时强制第二次容量抉择或产生重复账册条目。
- `StartErosion` **不重新快照**，仍为本事件首次开始时的值（`rewind.go:43`）。

### 6.4 幂等性

- 重复提交同一 `retries`：`current.Retries == retries + 1` → 直接 `return nil`（不改任何状态），HTTP 200（`rewind.go:27-29`；测试 `rewind_test.go:52-53`、`86-91`）。
- 会话不是 `failed`，或 `retries` 与当前值不符 → 409 `invalid_retry`，文案「事件状态已变化，请刷新后重试」（`rewind.go:25`、`30-32`、`50-56`）。
- 恢复与付款在**同一次 JSON 提交**内（`UpdateSessionAndPlayer` → 单次 `persistLocked`），不会出现「扣了钱没回溯」。

---

## 7. 描摹判定（trace）

参数由内容提供：`trace.aspect_ratio`、`trace.tolerance`、`trace.strokes`（`content.go:103-107`）。

### 7.1 容差与参数

| 项 | 值 | 依据 |
|---|---|---|
| 默认/实际 `tolerance` | **0.14**（以画布高度为单位），双字与单字均为 0.14 | `chapters.json`；文档 `content/README.md:31` |
| `tolerance` 合法区间 | `0 < tolerance <= 0.2` | `content.go:314` |
| `aspect_ratio` | 双字 **2.4**、单字 **1.2** | `chapters.json`（`tower_trace` 为 1.2，其余 2.4） |
| 笔数 | 11 / 11 / 15 / 18（双字），5（单字） | `chapters.json` |
| 采样点总量上限 | **8192** 点（超出则整题判失败） | `trace.go:21` |
| 相邻点最大跳距 | **0.12**（0–1 归一化单位，x 已乘 `aspect_ratio`） | `trace.go:46` |
| 单点最大偏离 | `tolerance * 1.6` = 0.224 | `trace.go:61` |
| 允许回描幅度 | `tolerance * 2` = 0.28 的进度回退 | `trace.go:61` |
| 允许出带采样点比例 | **≤ 10%**（`outside*10 > len(drawn)` 即失败） | `trace.go:64-69` |
| 最少采样点 | 每笔 **≥ 2** 点 | `trace.go:29` |
| 起笔约束 | 第一点投影进度必须 `<= tolerance`（必须从起点附近起笔） | `trace.go:61` |
| 收笔约束 | 最终进度必须 `>= 总弧长 - tolerance`（必须画到终点） | `trace.go:69` |

### 7.2 笔画校验维度（`matchStroke`，`trace.go:28-73`）

1. 笔数必须与 `spec.Strokes` 完全一致（少了记失败索引；多了则**所有**笔画都记失败，`trace.go:20-24`）。
2. 笔画顺序 = 数组顺序，逐笔按索引配对比对（`trace.go:20-22`）。
3. **方向**：由「进度单调不减（允许 2×容差回退）+ 终点必须到达」共同约束，反向绘制会被拒绝（`trace.go:61`、`69`；测试 `trace_test.go:22-27` `reverse` 用例）。
4. **跳点**：任一点与其前一点的欧氏距离 > 0.12 即失败（`trace.go:46`）。
5. **坐标合法性**：NaN / ±Inf / 越界 `[0,1]` 直接失败（`trace.go:43-45`）。
6. **连续性与覆盖**：允许手抖，但出主带的点不能超过 10%，且必须走完全程（`trace.go:64-69`）。
7. 逐笔判定彼此独立，**已通过的笔画不需要重画**（`trace_test.go:75-80`「repair of only missing stroke」）。

### 7.3 失败笔画反馈

- 返回 `failed_strokes`：**零基索引**数组，指向 `spec.Strokes` 中未通过的笔（`trace.go:14-26` → `rules.go:113-119` → `server.go:571`）。客户端显示为一基编号 + 红色 ×（`content/README.md:31`）。
- 描摹失败：`accepted=false`、`reason="trace_incomplete"`、**不加侵蚀、不计入 `InvalidAttempts`、不消耗 `max_attempts`**（`rules.go:115-120`；测试 `rules_test.go:63-80`、`httpapi/server_test.go:60-63` 断言 `erosion == 0` 且 `failed_strokes` 长度 11）。
- 描摹提交会把 `action` 改写为 `"trace"`，且 `AnswerHash = sha256(fmt.Sprint(strokes))`（`rules.go:103-106`）——答案/轨迹不会明文入库。

---

## 8. 结算公式（`calculateResult`，`server.go:935-971`）

入口：`POST /sessions/{id}/finish`；不接受客户端分数或奖励（`README.md:56`）。历次结果不重算，重复结算返回旧结果（`server.go:860-864`、`893-899`）。

### 8.1 四个分项

| 分项 | 公式 | 字段 | 权重 | 依据 |
|---|---|---|---|---|
| 修复度 repair | `PuzzleScore * 100 / PuzzleTotal`（整数除法，钳 0–100；`PuzzleTotal <= 0` 时为 0） | `repair_percent` | **40%** | `server.go:936-939` |
| 白蚀清除 cleared | 有战斗：`BattleWaves * 100 / Battle.Waves`；**无战斗：固定 100** | `battle_clear_percent` | **30%** | `server.go:940-943` |
| 记忆保留 retention | `len(player.Memories) * 100 / len(历史 kept 的唯一墨灵 ID)`；分母为 0 时 0 | `memory_retention_percent` | **30%** | `server.go:944-953` |
| 综合分 quality | `(repair*40 + cleared*30 + retention*30) / 100` | `quality_score` | — | `server.go:954` |

### 8.2 扣分与星级

```
quality -= min(20, InvalidAttempts * 5)      // 每次非法谜题尝试扣 5，最多扣 20
if (player.Erosion >= 70) quality -= 15      // 高侵蚀扣分，最多一次
quality = clamp(quality, 0, 100)

stars = 3  if quality >= 85
stars = 2  if 60 <= quality < 85
stars = 1  otherwise
```
依据 `server.go:955-965`。

| 结果字段 | 值 | 依据 |
|---|---|---|
| `stars` | 1–3 | `server.go:960-965` |
| `ink_marks_earned` | **= stars** | `server.go:970` |
| `scoring_version` | **2** | `server.go:966` |
| `puzzle_score` / `puzzle_total` | 事件内累计 | `server.go:968` |
| `memories_kept` / `memories_acquired` | 当前持有数 / 历史唯一 kept 数 | `server.go:967` |
| `erosion_at_end` | 结算时刻 `player.Erosion` | `server.go:970` |
| `sequence` | `player.LastSequence + 1`，随后写回 | `server.go:902-904` |
| `narrative_choice` | 终章叙事选项 | `server.go:901` |

**校验用例**（`scoring_review_test.go:9-24`）：repair=100、cleared=100、retention=70 → quality = (4000+3000+2100)/100 = **91** → 3 星；retention=10 → (4000+3000+300)/100 = **73** → 2 星。`repair_percent` 与 `quality_score` 明确解耦（测试第 23 行）。

### 8.3 事件内的「修复度」进度条（另一个量，勿与结算 `repair_percent` 混淆）

`session.RepairPercent` 是过程中的显示值：谜题完成时 = `halfPuzzlePercent` = `PuzzleScore * 50 / PuzzleTotal`（钳 0–50），战斗获胜时再 `+50`（`server.go:562`、`661-663`、`1092-1097`）。结算返回给客户端的 `result.repair_percent` 则是完整的 0–100 修复度。

---

## 9. 持久化

### 9.1 存档结构（`store.go:20-28`）

- **存档版本：`databaseVersion = 2`**（`store.go:20`）。
- 打开时**严格相等**校验，不匹配直接报错退出，**没有迁移逻辑**（`store.go:58-60`）；空文件也会报错而非静默重置（`store_test.go:55-63`）。

```
{
  "version": 2,
  "local_player_id": "<当前选中的单人存档 ID>",
  "accounts": <json.RawMessage，历史遗留，仅占位>,
  "players":  { "<id>": Player },
  "sessions": { "<id>": EventSession }
}
```

| 记录 | 字段 | 依据 |
|---|---|---|
| `Player` | `id, display_name, created_at, updated_at, ink_marks, capacity, erosion, unlocked_chapters[], completed_events{章节:事件 → EventResult}, memories[], memory_ledger[], last_sequence` | `model.go:7-20` |
| `Memory`（持有快照） | `id, title, summary, skill, capacity, source, added_at` | `model.go:22-30` |
| `LedgerEntry` | `memory_id, title, action(kept/forgotten/restored), chapter_id, event_id, occurred_at` | `model.go:32-39` |
| `EventResult` | `scoring_version, quality_score, memory_retention_percent, narrative_choice, memories_kept, memories_acquired, battle_clear_percent, chapter_id, event_id, stars, puzzle_score, puzzle_total, battle_won, repair_percent, erosion_at_end, ink_marks_earned, completed_at, sequence` | `model.go:41-60` |
| `EventSession` | `id, player_id, chapter_id, event_id, started_at, expires_at, last_seen_at, status, start_erosion, retries, attempt_count, invalid_attempts, puzzle_score, puzzle_total, accepted_steps[], actions[], battle_won, battle_checked, battle_waves, battle_hits, repair_percent, choice_done, choice_action, forgotten_memory, reward_applied, pending_memory, pending_result, failure_reason, narrative_choice` | `model.go:65-95` |
| `ActionRecord` | `step_id, action, answer_hash, accepted, reason, occurred_at` | `model.go:97-104` |

`ledger.action` 的 `"restored"` 取值在服务端**从未写入**——仅 `kept` 与 `forgotten`（`server.go:751`、`758`）。

### 9.2 事务边界

| 方法 | 语义 | 依据 |
|---|---|---|
| `persistLocked` | `MarshalIndent` → 写 `path + ".tmp"`（0600）→ `os.Rename` 原子替换；rename 失败则删临时文件 | `store.go:72-88` |
| `UpdatePlayer` | 克隆 → 变更 → 归一化 → 写盘；写失败则回滚内存对象 | `store.go:127-146` |
| `UpdateSession` | 同上（会话） | `store.go:197-215` |
| `UpdateSessionAndPlayer` | **会话 + 玩家同一次写盘**，任一失败双方回滚；用于谜题、战斗、抉择、回溯等增量操作 | `store.go:252-279` |
| `FinalizeSession` | **结算专用**：会话状态迁移 + 玩家奖励（墨痕 / 容量 / `CompletedEvents` / 章节解锁）在一次写盘内完成，防止断电只写一边 | `store.go:220-247` |
| 并发 | 单进程单 `sync.RWMutex`（`store.go:30-34`）全量串行化；`StartSession` 选会话与建会话共用同一把锁，避免并发开局分叉 | `store.go:160-166` |

### 9.3 归一化与默认值（`normalizePlayer`，`store.go:300-325`）

`capacity <= 0 → 5`；`ink_marks < 0 → 0`；`erosion < 0 → 0`；`erosion > 100 → 100`；`unlocked_chapters` 为 nil → `["prologue"]`；三个集合字段 nil → 空集合。

单存档选择：`LocalPlayer()` 优先 `local_player_id`，否则取 `updated_at` 最新（同时间取 ID 字典序最小），全新建档时创建 `local-player` + 显示名「拓印师」（`store.go:379-405`）。

---

## 10. 内容协议版本

| 项 | 值 | 依据 |
|---|---|---|
| 当前版本 | **10**（`content.CurrentVersion`） | `content.go:14` |
| 兼容规则 | **严格相等**：`c.Version != CurrentVersion` 即加载失败，无向前/向后兼容、无迁移 | `content.go:208-210` |
| 加载失败时机 | 进程启动即 `log.Fatalf` | `main.go:41-44` |
| 健康检查暴露版本 | `/healthz → content_version` | `server.go:103-112` |
| 文档 | `content/README.md:3` 标注 v10 | — |

`GET /api/v1/catalog` 下发公开目录（`server.go:183-202`）：`skills`、`memories`（含 `image`、`remembered_text` 等）、`failure_scene`、`art`、`chapters[].events[]`（含 `story`、`investigations`、`objectives`、`puzzle.steps[]` 的 `id/kind/prompt/options/points/trace`、`battle` 全量、`reward`）。
**`puzzle.steps[].answer` 不下发**（`publicPuzzleStep` 结构体无 `Answer` 字段，`server.go:165-172`；测试 `server_test.go:54-57` 断言响应中不含 `"answer"`）。`target` 在公开目录中也不下发（`server.go:165-172`）。

加载期集中校验（`content.go:190-370`）要点：failure_scene 必须含 title / `res://assets/` 背景 / 非空对白 / 4 条 `first_lines`（`erosion_limit`、`puzzle_attempt_limit`、`battle_requirements_not_met`、`session_expired`）；`skills` 必须含 `挥墨`；可玩事件必须有 `art[event_id].backdrop_color` + `whisper_audio`；墨灵必须存在且 `source == 章节ID/事件ID`、必须含 4 段文字、`image` 必须是 `res://assets/rubbings/*.png`；事件必须有 beats/outro/dialogue，且 `len(clues) == len(investigations)`；谜题步骤 ID 唯一、非 trace/narrative 步骤必须有 `answer`、`skill` 步骤 target 限 `beam|bell|inscription`、`narrative` 至少 2 个选项、trace 线条坐标必须在 `[0,1]` 且无重复顶点。

---

## 11. 实现与策划的已知落差

### A. 有字段/文案但**没有真实逻辑**（仅字段/未实现）

1. **`session_expired` 永不产生**。它只作为失败演出文案键存在：内容必须配置该键（`content.go:202-207`）、客户端有映射（`客户端/scripts/main.gd:1200`），但服务端**没有任何代码设置该失败原因**；`EventSession.ExpiresAt` 从不在服务端赋值（`server.go:392-398`），下发恒为 `0001-01-01T00:00:00Z`。`server_test.go:125-160` 还专门断言过期时间不影响通关。→ 上一代「墙钟超时」机制的残留。
2. **`awaiting_choice` 状态从未被写入**。四态注释（`model.go:76`）中只有 active/completed/failed 真实存在；`store.go:285` 仍在统计它。
3. **`RewardSpec.UnlockChapter`（`reward.unlock_chapter`）无任何引用**（`content.go:154`）。章节解锁实际走 `chapter.next_chapter`（`server.go:1060-1079`）。内容中该字段全为空。
4. **`PuzzleSpec.Type`（`puzzle.type`）不参与逻辑**，只做下发（`server.go:160`）；服务端一律按 `step.kind` 判定。内容里 `purify_trace`/`pattern`/`combo` 等名称没有任何服务端分支。
5. **`battleRequest.WavesCleared` / `HitsTaken` 被接收后丢弃**（`server.go:583-584`、`643` → `rules.go:179-190` 未读取）。客户端自报的战果字段是诱饵；服务端完全重放。
6. **`MemorySpec.ToneHz`（`tone_hz`）仅字段**（`content.go:165`），`content/README.md:9` 明确「不再用于播放」。
7. **`InvestigationSpec`（调查线索）只有数据**：仅做坐标合法性校验（`content.go:292-296`）与下发，服务端**没有任何调查交互接口**；`clues` / `objectives` / `story.beats` / `dialogue` / `first_clear_whisper` 全是纯展示数据（无逻辑引用）。
8. **`LedgerEntry.Action = "restored"`** 在 `model.go:35` 注释里被列为合法值，服务端从不写入。
9. **死代码/死分支**：`maxInt`（`server.go:1191-1196`）定义后无调用；`store.database.databaseVersion` 的 `accounts` 字段（`LegacyAccounts`，`store.go:23`）只是历史占位；`startSession` 的 `draft` 拦截（`server.go:367-370`）与 `unlockNextChapter` 的 `next.Events[0].Draft` 拦截（`server.go:1074-1076`）在当前内容（全部 `draft=false`）下永不触发。
10. **无鉴权代码**：`鉴权与第三方接入.md` 自述为历史归档，其中描述的 `internal/httpapi/auth.go`、`IdentityProvider`、`httpapi.New(catalog, database, adapter)` 三参签名在当前代码库中均不存在（`server.go:46` 为两参）；`/api/v1/auth/login` 返回 404（`story_test.go:41-43` 断言）。

### B. 临时 / 占位 / 与文档不一致

11. **`content/README.md` 与 `服务端/README.md` 的版本号过期**：代码为 v10（`content.go:14`），而 `服务端/README.md:50` 写「内容协议为版本 9」、`README.md:66` 标题写「回溯与继续（协议 9）」。另有 `鉴权与第三方接入.md:1` 写「内容协议 5」。
12. **所有章节解锁费为 0** → 经济系统的「章节解锁」支路（`server.go:1071-1077`）实际是空转，墨痕只出不进只有回溯一处；这与「以墨痕买章节」的直觉设计存在明显落差，看起来是为后续章节预留但未启用。
13. **`闪身` 是孤儿技能**：目录中定义了 description/冷却/护盾（`chapters.json:2191-2192`），但没有任何墨灵授予、没有 `required_skills` 引用；它的可用性只靠 `server.go:635-636` 的硬编码白名单。等价于一个「所有人开局赠送」的免费减伤。
14. **`max_attempts` 的默认值是在请求路径里回写共享目录**：`startSession` 直接改 `event.Puzzle.MaxAttempts = 3`（`server.go:375-377`），而 `event` 指向 `s.catalog` 的内部切片——这是并发请求下的数据竞争，也说明「默认 3 次」本该写在内容或加载期而非 handler。
15. **护盾不叠加（`max`）与治疗等价于冲抵受击数**（`rules.go:241-244`）与「盾可以叠」的常见预期不同；`斗拱`(2 盾) 与 `闪身`(1 盾) 组合时后放的低盾值会被忽略，而治疗只是在账面上退还侵蚀额度，战斗回放里没有真正的血量。
16. **`retention` 用「当前持有数 / 历史获得数」**（`server.go:944-953`），这意味着**主动遗忘会永久拉低当前事件的综合分**；同时该指标在不同事件之间不重置分母（分母是玩家全生命周期的 kept 唯一 ID 集合），后期事件天然更难拿 3 星。
17. **`RepairPercent` 一物两义**：会话内是 0–50 的谜题进度（加上战斗胜利的 50 封顶 100），结算结果里是 0–100 的修复度（`server.go:562/661-663` vs `936-939`）。字段同名同类型但语义不同，是容易误用的接口设计。
18. **`tower_epilogue` 墨灵 `skill` 为空串**：终章墨灵不授予任何技能（`chapters.json memories.tower_epilogue.skill == ""`），验证期也允许空技能（`content.go:255-257`），但玩家仍要为首尾章的那一格容量付出抉择成本。
19. **`max_hits_taken` 全事件统一为 12、`duration_sec` 统一为 60、普通波攻击间隔硬编码 3000 ms**（`content.go:143-148`）：8 场战斗的难度差异几乎只由 `enemy_hp`/`waves`/`required_skills` 决定，攻击节奏参数是写死的，看起来是占位实现（策划案若给定不同节奏则无法只靠 JSON 表达）。
20. **`storage` 无版本迁移**：`databaseVersion` 严格相等（`store.go:58-60`），存档结构一旦变更，旧存档直接导致服务启动失败，而不是降级/迁移——对比赛场景可接受，对发行是硬伤。
