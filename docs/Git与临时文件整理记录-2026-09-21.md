# Git 与临时文件整理记录

## 大文件与完整性

核验范围为当前工作区可提交文件、Git 暂存区，以及本机全部分支和标签可达的历史对象。检查结果：没有达到 100 MiB 的待提交文件或历史 blob；未获取的远端对象不在范围内。新增 `tools/check_git_size.py` 可重复执行该检查。

GitHub 普通 Git 阻止超过 100 MiB 的单文件，50 MiB 以上会警告。`.gitignore` 不能按文件大小过滤，不能代替体积检查。[官方说明](https://docs.github.com/en/repositories/working-with-files/managing-large-files/about-large-files-on-github)

| 内容 | 处理 |
| --- | --- |
| 约 674 MiB 的完整参赛 ZIP | 保留本地，精确忽略 `output/参赛材料/*.zip`；同名 SHA256 与核验 JSON 保留 |
| 约 144 MiB 的展开客户端 EXE | 保留本地且忽略；可从当前发行 ZIP 恢复 |
| 当前完整游戏发行 ZIP | 92,903,008 字节（88.60 MiB），允许入库；CRC 和既有 SHA256 核验通过 |
| 正式 Word/PDF、JPG、素材、源码、AI 会话 | 保留；未整体忽略 output、图片、音频、日志或交流记录 |
| 最大 AI JSONL | 62.52 MiB，低于限制；为完整保留记录不裁剪，可能出现 50 MiB 警告 |
| Git 历史 | 复核时已包含当前发行 ZIP，最大 blob 88.60 MiB，无需重写历史或迁移 LFS |

总材料 ZIP 已移除缩略图缓存并重建文件清单，最新 SHA256 见 `output/参赛材料/本科组_Track1_第二队.zip.sha256`，完整校验结果见同目录 `压缩包核验结果.json`。正式 Word/PDF、图片素材、源码、可玩程序和 AI 记录未删改。本次补充测试原始记录位于开发目录，不改写既有测试结论。

## 忽略规则修正

- 移除对 Godot `.uid` 的忽略，保留资源身份元数据；`.godot` 缓存仍忽略。依据 [Godot 官方 UID 说明](https://godotengine.org/article/uid-changes-coming-to-godot-4-4/)。
- 将 `dist` 从整体忽略改为默认忽略内部产物，并精确放行当前发行 ZIP、校验值、说明及 `.gitkeep`。
- `LLM-tmp/04-工具` 中保留工具源码，只忽略可重新抽取的 `tech-full.txt`。
- 添加 Python 缓存、Office 锁文件和系统缩略图的忽略规则。
- 6 张已跟踪的临时 PDF 渲染图、`tech-full.txt` 与 2 份 `Thumbs.db` 均已取消跟踪并删除本地副本。参赛副本的文件清单和总 ZIP 已同步重建。

## 临时文件清理状态

从旧 `.review` 目录筛出 166 个尚未重复的测试记录文件，按原目录归档至 `LLM-tmp/验证记录/2026-09-15-可玩性/补充原始记录`（65,260,686 字节）。逐文件来源、保存路径、大小和 SHA256 见其中 `归档索引.json`。重复记录、测试 APPDATA、编译程序不再额外归档。历史测试日期仍为 9 月 15 日。

权限设置生效后，清理脚本已获准执行成功。实际删除 `tmp`、三份已解压工具安装 ZIP、Godot 缓存、PDF 抽取文本、已归档的 `.review` 工作目录及两份缩略图缓存，共 9 个明确目标、2,459,554,499 字节（约 2.29 GiB）。工具本体保留用于现有构建配置。

清理脚本会先核验游戏发行 ZIP 和归档证据哈希，再检查每个删除目标仍位于项目根目录内，遇到链接目录则停止。不会删除源码、原始素材、正式 AI 记录、可玩发行包或总参赛 ZIP。

本次清理已完成。今后需要预览同一范围时，可在仓库根目录执行：

```powershell
# 仅预览
pwsh -NoProfile -File ./tools/clean_submission_temp.ps1 -WhatIf
# 确认预览范围后执行
pwsh -NoProfile -File ./tools/clean_submission_temp.ps1
```

助手本次没有提交、推送或改写 Git 历史。复核时仓库已有 `cda1e77 Complete Push`，已包含此前的 Git 整理；本轮只留下清理结果说明、打包工具、忽略规则及更新后的参赛包校验信息等变更。

需要重新生成本地总提交 ZIP 时，先按 `dist/README.md` 恢复完整运行文件，然后使用 Python 3.11 或更新版本执行 `python tools/package_submission.py`。打包使用标准库，会先检查临时文件和可玩实体存在性，校验新 ZIP 的 CRC 与全部清单哈希，通过后才替换旧 ZIP。
